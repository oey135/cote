class Solution {
    public int solution(int[] sides) {
        int answer = 0;
        int big=0;
        for(int i=0; i<sides.length; i++) {
            big < sides[i] ? : sides[i];
        }
        
        return answer;
    }
}